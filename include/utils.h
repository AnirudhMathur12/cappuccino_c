#ifndef UTILS_H_
#define UTILS_H_
#include <memory.h>
#include <stdio.h>
#include <stdlib.h>

#define MAX_ARR 10000

char *read_file(const char *path);
int isalphanum(char c);

typedef struct {


} dynamic_array;

#endif
